-- Deals with project listing and navigation
local config = require("projection.config")
local path = require("utils.path")

local M = {}

M.list_projects = function() end

-- Change project dir based on selection
M.change_project_dir = function() end

return M
